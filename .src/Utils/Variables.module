' Gambas module file

Public son As String = Settings["/son"]
Public Sprix As Boolean = Settings["/Soc" & Start.Societe & "/Sprix"]
Public Sexport As Boolean = Settings["/Soc" & Start.Societe & "/Sexport"]
Public Simport As Boolean = Settings["/Soc" & Start.Societe & "/Simport"]
Public Schmprix As String = Settings["/Soc" & Start.Societe & "/Schmprix"]
Public SMajArt As Boolean = Settings["/Soc" & Start.Societe & "/SMajArt"]
Public SMajFour As Boolean = Settings["/Soc" & Start.Societe & "/SMajFour"]
Public SMajCli As Boolean = Settings["/Soc" & Start.Societe & "/SMajCli"]
Public Musique As String = "son/warning.ogg"
Public OrgTarCli As String = ""
Public OrgTarTypeCli As String = ""
Public OrgCoefTypeCli As String = ""
Public $TarTypeCli As String = ""
Public ArtCode As String = ""
Public Artintitule As String = ""
Public ArtTva As String = ""
Public ArtHt As String = ""
Public TypeL As String
Public Codearticle As String
Public Cfour As String
Public obs As String
Public $fic As String
Public $doc As String
Public EnvoiMail As String
Public $Codeclient As String
Public $Codearticle As String
Public $Codemo As String
Public numeroFac As String
Public codepostal As String
Public Bureaudistributeur As String
Public numerobl As String[]
Public inumr As Integer
Public Qte As Float
Public bRemise As Boolean = False
Public Bselection As Boolean = False
Public Bsel As Boolean = False
Public FMateriel As Boolean = False
Public Version As String
Public impr As String
Public bImpref As Boolean
Private hpb As ProgressBar
Private hlb As Label

Public Sub RecupVersion()

  Dim rResult As Result

  rResult = Utils.db.Exec("SELECT * FROM Fiches_Parametres ")
  Try Version = rResult!version
  If Settings["/Majver"] Then Verif_Maj()

End

Public Sub Verif_Maj()

  Dim txt As String
  Dim $hform As Form
  Dim hLabel As Label

  $hForm = New Form As "Form"
  $hform.Width = 600
  $hform.Height = 400
  $hForm.Border = Border.Etched
  $hform.Title = "Mise à jour de Laurux"
  hLabel = New Label($hForm)
  hLabel.Text = "La mise à jour prend un peu de temps.\n Veuillez patienter SVP."
  hLabel.Width = 600
  hLabel.Alignment = Align.center
  hLabel.Height = 40
  hLabel.Y = 12
  'hLabel.Border = Border.Plain
  hlb = New Label($hForm)
  hlb.Width = 600
  hlb.Height = 20
  hlb.Y = 70
  hlb.Alignment = Align.center
  hpb = New ProgressBar($hForm)
  hpb.Width = 400
  hpb.Height = 20
  hpb.X = 100
  hpb.Y = 100
  $hForm.Move($hForm.ScreenX, $hForm.ScreenY, 600, 200)
  $hform.Center
  If Exist(User.Home & "/tmp/pb1.txt") Then Kill User.Home & "/tmp/pb1.txt"
  If Exist("/version.txt") Then Kill "/version.txt"
  If Exist("/Laurux3.tar.gz") Then Kill "/Laurux3.tar.gz"
  If Exist(User.Home & "/tmp/ping.txt") Then Kill User.Home & "/tmp/ping.txt"
  Shell "ping -c 1 www.laurux.fr >" & User.Home & "/tmp/ping.txt 2>&1" Wait
  If Exist(User.Home & "/tmp/ping.txt") Then
    txt = Trim$(File.Load(User.Home & "/tmp/ping.txt"))
  Endif
  If InStr(txt, "cpanel1") <> 0 Then
    Shell "wget -q" & " " & "http://www.laurux.fr/file/version.txt" & " -P " & User.Home & "/tmp" Wait
    If Exist(User.Home & "/tmp/version.txt") Then
      txt = Trim$(File.Load(User.Home & "/tmp/version.txt"))
      If IsNull(Variables.Version) And Not IsNull(txt) Or If Not IsNull(Right$(Variables.Version, 4)) And Not IsNull(txt) Then
        If Replace$(Right$(txt, 4), ",", ".") <> Variables.Version Then
          If Message.Question("Attention ! Vous allez mettre à jour votre logiciel Laurux\n Vous devez être connecté au réseau internet. \n Etes-vous d'accord ?", "Oui", "Non") = "1" Then
            Try Move Application.Path & "/Laurux3.gambas" To Application.Path & "/Laurux3.old.gambas"
            Shell "cd " & User.Home
            $hForm.Show
            Wait 1
            $hform.Mouse = Mouse.Wait
            Shell "wget http://www.laurux.fr/file/Laurux3.tar.gz -P " & User.Home & " -o " & User.Home & "/tmp/pb1.txt"
            Avancement()
            Shell "cd " & User.Home & " " & "; tar xvfz " & User.Home & "/Laurux3.tar.gz" Wait
            Wait 1
            Kill User.Home & "/Laurux3.tar.gz"
            Kill User.Home & "/tmp/version.txt"
            $hform.Mouse = Mouse.Default
            $hform.Close
            Settings["/Affmaj"] = 1
            Settings.Save
            Message.Warning("Mise à jour terminée ! Veuillez relancer Laurux SVP !")
            Quit
          Endif
        Endif
      Endif
    Else
      $hform.Mouse = Mouse.Default
      Message.Warning("Problème de mise à jour! Veuillez vérifier votre connexion SVP.")
    Endif
  Else
    $hform.Mouse = Mouse.Default
    Message.Warning("Problème de mise à jour! Veuillez vérifier votre connexion SVP.")
  Endif
  'Catch
  '  Music.Load(Start.Musique)
  '  Music.Play
  ' message.Error(Error.Text & " " & Error.where)

End

Public Sub Avancement()

  Dim hFile As File
  Dim chaine As String
  Dim v As Integer
  Dim numligne As Integer
  Dim L3 As String = "0"

  numligne = 0
  v = 0
  Do While Not Exist(User.Home & "/tmp/pb1.txt")  'On test l'existence du fichier temporaire
  Loop
  Wait 0.1
  Do While L3 <> "100"      'On test la fin du téléchargement
    hFile = Open User.Home & "/tmp/pb1.txt" For Input
    numligne = 0
    While Not Eof(hFile)
      Line Input #hFile, chaine
      If v = 100 Then L3 = "100"
      If Mid(chaine, 7, 2) = "K " And Len(chaine) <= 78 And Len(chaine) >= 75 Then
        L3 = Mid(chaine, 63, 3)
        If CInt(L3) > v Then  'On incrémente la barre de progression
          v = CInt(L3)
          Hpb.Value = CInt(L3) / 100
          hlb.Text = Chaine
        Endif
      Else
        Wait
      Endif
    Wend
    Close #hFile
  Loop
  'CLOSE #hFile
  Try Kill User.Home & "/tmp/pb1.txt"  '--- Suppression du fichier temporaire ---

End
