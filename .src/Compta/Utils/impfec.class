' Gambas class file

Private $hfile As File
Private $fname As String

Public Sub _new()

  Dim ch As Chainage
  Dim s As String
  
  
  FileChooser1.SetFocus
  ch = New Chainage([TextReg1, TextReg2, TextReg3, TextReg4, TextReg5, TextReg6, TextReg7, TextReg8, TextReg9, TextReg10, TextReg11, TextReg12, TextReg13, TextReg14, TextReg15, TextReg16, TextReg17, TextReg18, TextReg19, TextReg20])
  

End


Public Sub Button1_Click()

 
  
  If Not ctrl() Then Return

End

Private Function ctrl() As Boolean
  
  $hfile = Open FileChooser1.Value For Read
  
  
End

Public Sub FileChooser1_LostFocus()

  If Not FileChooser1.SelectedPath Then
    Message.Info("Vous devez choisir un fichier")
    FileChooser1.SetFocus
  Endif
  If Not Exist(FileChooser1.SelectedPath) Then
    Message.Info("Ce fichier n'existe pas")
    FileChooser1.SetFocus
  Endif
End
